# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(envire_mapsTaskLib)
ADD_LIBRARY(${ENVIRE_MAPS_TASKLIB_NAME} SHARED 
    ${ENVIRE_MAPS_TASKLIB_SOURCES})
add_dependencies(${ENVIRE_MAPS_TASKLIB_NAME}
    regen-typekit)

TARGET_LINK_LIBRARIES(${ENVIRE_MAPS_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${ENVIRE_MAPS_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${ENVIRE_MAPS_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${ENVIRE_MAPS_TASKLIB_INTERFACE_LIBRARIES}")
SET_TARGET_PROPERTIES(${ENVIRE_MAPS_TASKLIB_NAME}
    PROPERTIES INTERFACE_LINK_LIBRARIES "${ENVIRE_MAPS_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${ENVIRE_MAPS_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${ENVIRE_MAPS_TASKLIB_HEADERS}
    DESTINATION include/orocos/envire_maps)

set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -std=c++11")