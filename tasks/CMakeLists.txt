# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(mapsTaskLib)
ADD_LIBRARY(${MAPS_TASKLIB_NAME} SHARED 
    ${MAPS_TASKLIB_SOURCES})
add_dependencies(${MAPS_TASKLIB_NAME}
    regen-typekit)

TARGET_LINK_LIBRARIES(${MAPS_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${MAPS_TASKLIB_DEPENDENT_LIBRARIES}
    maps-viz vizkit3d
    )
SET_TARGET_PROPERTIES(${MAPS_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${MAPS_TASKLIB_INTERFACE_LIBRARIES}")
SET_TARGET_PROPERTIES(${MAPS_TASKLIB_NAME}
    PROPERTIES INTERFACE_LINK_LIBRARIES "${MAPS_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${MAPS_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${MAPS_TASKLIB_HEADERS}
    DESTINATION include/orocos/maps)

set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -std=c++11")